% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait_analysis.R
\name{analyze_hscore_changes}
\alias{analyze_hscore_changes}
\title{Analyze differential glycan trait means and heterogeneity}
\usage{
analyze_hscore_changes(
  gpsm,
  from,
  motifs = NULL,
  meta,
  group_col,
  group_levels,
  B = 1000,
  min_samples = 3,
  method = c("permutation", "t.test")
)
}
\arguments{
\item{gpsm}{A GPSM table imported by
\code{\link{read_pGlyco3_gpsm}} or
\code{\link{read_decipher_gpsm}}.}

\item{from}{Character string specifying the glycan representation.
One of \code{"pGlyco3"} or \code{"decipher"}.}

\item{motifs}{Optional glycan motif definitions.}

\item{meta}{Sample metadata table.}

\item{group_col}{Column name in \code{meta} defining the comparison groups.}

\item{group_levels}{Length-2 character vector specifying the two groups
to compare.}

\item{B}{Integer; number of random label permutations used when
`method = "permutation"`. Default is 1000.}

\item{min_samples}{Minimum number of non-missing samples required in each
group.}

\item{method}{Statistical test used for group comparison. One of
`"permutation"` or `"t.test"`. Default is `"permutation"`.}
}
\value{
A data frame containing differential analysis results for all glycan traits.
}
\description{
A high-level wrapper that computes glycan trait abundance and heterogeneity
scores from GPSM data and tests for differences between two experimental
groups.

The function internally:
\enumerate{
  \item Computes site- and protein-level glycan heterogeneity scores.
  \item Performs permutation-based or Welch's t-test group comparisons.
}
}
\examples{
gpsm <- readRDS(system.file("extdata", "gpsm_toyexample.rds", package = "glycoTraitR"))
meta <- readRDS(system.file("extdata", "meta_toyexample.rds", package = "glycoTraitR"))

res <- analyze_hscore_changes(
  gpsm = gpsm,
  from = "pGlyco3",
  meta = meta,
  group_col = "Diagnosis",
  group_levels = c("Normal", "Symptomatic"),
  B = 5
)

head(res)

}
