% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_glyco_traits.R
\name{plot_trait_distribution}
\alias{plot_trait_distribution}
\title{Plot the Distribution of a Glycan Trait Across Experimental Groups}
\usage{
plot_trait_distribution(trait_se, group_col, group_levels, trait_name, feature)
}
\arguments{
\item{trait_se}{A `SummarizedExperiment` produced by `build_trait_se()`, containing
glycan traits in its `assays()`.}

\item{group_col}{A character string giving the column name in `colData(trait_se)`
that defines group membership.}

\item{group_levels}{A character vector specifying the values within `group_col` that
should be plotted (e.g., `c("Control", "Treatment")`).}

\item{trait_name}{Name of the glycan trait to visualize. Must match one of the assay
names in `assays(trait_se)`.}

\item{feature}{Row identifier (e.g., peptide, protein, or site label) indicating
which feature to plot. Must match row names of the selected trait
matrix.}
}
\value{
A named list with two `ggplot2` objects:

* `p_hist` — group-wise histogram of trait abundance
* `p_box`  — boxplot with jitter showing group-level differences
}
\description{
Generate two diagnostic plots—histogram and boxplot—for a specific glycan
trait measured at a selected glycosylation site or protein level.

The function extracts a trait vector from a `SummarizedExperiment` object,
subsets PSMs according to the specified grouping variables, removes missing
values, and visualizes the trait distribution across groups.
}
\details{
Two plots are returned:

* **Histogram** showing distribution of trait abundance across PSMs
* **Boxplot + jitter** summarizing group-level differences

Only non-`NA` PSMs are included.
}
\examples{
\dontrun{
res <- plot_trait_distribution(
  trait_se     = se,
  group_col    = "condition",
  group_levels = c("Control", "Treatment"),
  trait_name   = "Antennas",
  feature      = "PEPTIDE_12"
)

res$p_hist
res$p_box
}

}
